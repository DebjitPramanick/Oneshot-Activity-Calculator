export interface IntroLabelsType {
    shouldTry_label: string;
    shouldTryButton_label: string;
    shouldReveal_label: string;
    shouldRevealButton_label: string;
}

export interface InputLabelsType {
    heading_label: string;
    subheading_label: string;
    deal_label: string;
    revGoal_label: string;
    opp_deal_label: string;
    meet_opp_label: string;
    leads_meet_label: string;
}

export interface OutputLabelsType {
    customers_label: string;
    opportunities_label: string;
    meetings_label: string;
    leads_label: string;
    emails_label: string;
    demoQuestion_label: string
}